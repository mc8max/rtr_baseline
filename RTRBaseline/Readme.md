# RTRBaseline (Metal + SwiftUI + C++ Core)

A clean baseline repository for learning **Real-Time Rendering** on Apple platforms using:
- **SwiftUI** app shell
- **MetalKit (MTKView)** for rendering
- A small **C++ core** for math + mesh data
- An **Objective-C++ bridge** so Swift can call into the C++ core cleanly
